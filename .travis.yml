matrix:
  include:
    - language: c++
      before_script:
        - cd client
      script:
        - ./build.sh && ./test.sh && ./done-hook.sh

    - language: go
      go:
        - "1.9"
      before_script:
        - cd backend
      script:
        - go build && go test 

